/*// Задачи по 2 главе

//Типы данных

let name = "Ilya";

console.log( `hello ${1}` ); // hello 1

console.log( `hello ${"name"}` ); // hello undefined - ошибся

console.log( `hello ${name}` ); // hello ilya


//1 Задание: укажи, какой тип данных у каждого значения.

let a = "Привет"; //string

let b = 42; //number

let c = 42n; //bigint

let d = true; //boolean

let e; //undefined

let f = null; //object

let g = Symbol("id");

//2 Ниже приведён код, который запрашивает у пользователя два числа и показывает их сумму.
//   Он работает неправильно. Код в примере выводит 12 (для значения полей по умолчанию).
// В чём ошибка? Исправьте её. Результат должен быть chapter3.

let a = +prompt("Первое число?", 1);
let b = +prompt("Второе число?", 2);

alert(a + b); // 12

// Задача chapter3 — Типы данных и alert/prompt/confirm
//
// Напиши скрипт, который спрашивает имя пользователя через prompt, возраст через prompt, и выводит alert:

let user = prompt('Как тебя зовут?')

let year = +prompt('Сколько тебе лет')

alert(`Привет, ${user}! Через 10 лет тебе будет ${year + 10}`)


// Задача 4 — Преобразование типов
//
// Что будет выведено в консоль:

console.log(String(123) + 4); // "1234" здесь 123 преобразует в строку метод String и прибавляет к этой строке 4
console.log(Number("5") + chapter3); //8 метод Number преобразует 5 в число если два аперанда Number то бинарный оператор + складывает значения
console.log(Boolean(0));//false тут все логично 1 - true 0 false
console.log(Boolean("hello"));//true здесь строке не пустая значит true


// Задача 5 — Базовые операторы и математика
// Напиши выражение, которое:
// Делит 15 на 4, округляет вниз до целого числа, добавляет 2, потом возводит в квадрат.
// Выведи результат в console.log.


let result = (Math.floor(15 / 4) + 2) ** 2
console.log(result)


// Задача 6 — Операторы сравнения
//
// Предскажи результат:

console.log("5" > 2); //true при сравнение числа и строки, строка приводится к Number и результат будет
console.log("5" < "12"); // false здесь идет сравнение посимвольно 1 не может быть больше 5
console.log(null == 0); //false -- не понимаю логики его
console.log(null >= 0);
console.log(undefined > 0); //false сравнение с undefined приводит к NaN


/!*
Задача 7 — Условное ветвление (if и ?)

  Напиши код, который:

спрашивает число через prompt

если число > 0 → alert("Положительное")

если число < 0 → alert("Отрицательное")

если равно 0 → alert("Ноль")

Реализуй ещё версию через тернарный оператор ?, где выводится одна строка с результатом.*!/


const num = +prompt('Введите число', '0')

if (num > 0) alert("Положительное")

else if (num < 0) alert("Отрицательное")

else alert("Ноль")


num > 0 ? alert('Положительное') :
  num < 0 ? alert("Отрицательное") :
    alert("Ноль")


// Задача 8 — Логические операторы
//
// Предскажи результат:

console.log(true && "yes"); // "Yes"// && то есть и cлева и справа должно быть true, первое true - true, в строке что-то есть значит тоже true
console.log(false || "no"); // "No" // || либо слева, либо справа ищет true значение
console.log(!0); //true//  0 - это false по умолчанию, ! - делает реверс булевого значения
console.log(!!""); //false// !! - просто показывает какое булевое значение
console.log(null || undefined || "default");//"default"// здесь логика такая же как во втором идем с первого спотыкаемся об true


// Задача 9 — Операторы нулевого слияния и присваивания (??, ??=)
//
// Есть переменная let a;
//
// Используй ?? для присвоения 10, если a не задано.
//
//  Используй ??= для того же, если переменная может быть undefined.

  let a;
a = a ?? 10;
console.log(a); //10 потому что a = undefined оператор нулевого слияния нам говорит, если к примеру присутствует null или undefined верни значение справа


// Блок 1 — ?? (нулевое слияние)
// Задача 1
// Что будет в result? Объясни словами.

let value = 0;
let result = value ?? 100; //вернет 0 так как value не является не null или undefined

console.log(result)

let value = undefined;
let result = value ?? "default";//вернет "default" так как value  является  null или undefined


// Задача chapter3 (ловушка)

let value = "";
let result = value ?? "empty"; //Вернет пустую строку так как это не null и не undefined
console.log(result)


// Блок 2 — сравнение с || (чтобы почувствовать разницу)
//
// Задача 4

let value = 0;
let result = value || 100; //вернет 100 так как || проверяет false/true соответственно 100 это true
console.log(result)


let a;
a ??= 10; //присвой переменной а 10 если а будет null или undefined

console.log(a)


let a = null;
a ??= "hello"; // тоже самое ответ "hello"*/


// Выведите чётные числа

// При помощи цикла for выведите чётные числа от 2 до 10.

// for (let i = 1; i <= 10; i++) {
//   if (i % 2 === 0) {
//     console.log(i)
//   }
// }


// Замените for на while

// Перепишите код, заменив цикл for на while, без изменения поведения цикла.
// let i = 0
// while (i < chapter3) {
//   console.log(`number ${i}!`);
//   i++
// }

// Повторять цикл, пока ввод неверен

// Напишите цикл, который предлагает prompt ввести число, большее 100. Если посетитель ввёл другое число – попросить ввести ещё раз, и так далее.
//
//   Цикл должен спрашивать число пока либо посетитель не введёт число, большее 100, либо не нажмёт кнопку Отмена (ESC).
//
//   Предполагается, что посетитель вводит только числа. Предусматривать обработку нечисловых строк в этой задаче необязательно.//

// for (let i = 0; i < 100; i++) {
//
//   i = prompt('Наберите число больше 100')
//
//   if (i === null) {
//     break
//   }
// }

// Напишите "if", аналогичный "switch"
// важность: 5
// Напишите if..else, соответствующий следующему switch:
//
//   switch (browser) {
//     case 'Edge':
//       alert( "You've got the Edge!" );
//       break;
//
//     case 'Chrome':
//     case 'Firefox':
//     case 'Safari':
//     case 'Opera':
//       alert( 'Okay we support these browsers too' );
//       break;
//
//     default:
//       alert( 'We hope that this page looks ok!' );
//   }

// let browser = prompt('enter the name of the browser you are using')
//
// if (browser === 'Edge') {
//   alert('You ve got the Edge!')
// } else if (browser === 'Chrome' || browser === 'Firefox' || browser === 'Safari' || browser === 'Opera') {
//   alert('Okay we support these browsers too')
// } else {
//   alert('We hope that this page looks ok!');
// }


// Перепишите код с использованием одной конструкции switch:
//
//   const number = +prompt('Введите число между 0 и chapter3', '');
//
//   if (number === 0) {
//     alert('Вы ввели число 0');
//   }
//
//   if (number === 1) {
//     alert('Вы ввели число 1');
//   }
//
//   if (number === 2 || number === chapter3) {
//     alert('Вы ввели число 2, а может и chapter3');
//   }

// const number = +prompt('Введите число между 0 и chapter3', '');
//
// switch (number) {
//   case 0:
//     alert('Вы ввели число 0');
//     break;
//   case 1:
//     alert('Вы ввели число 1');
//     break;
//   case 2:
//   case chapter3:
//     alert('Вы ввели число 2, а может и chapter3');
//     break;
//   default:
//     alert('Вы ввели отрицательное или больше chapter3+')
// }











// Задачи с gpt по 2 главе

// 🧠 Задача 1 — переменные и типы
//
// Посмотри на код:
//
//   let a = "5";
// let b = 2;
//
// let result = a + b;
//
//
// Вопрос (один!):
// 👉 Какое значение будет в result и почему?

//ответ: '52' так как один операнд является строкой, а второй number, num преобразуется в строку и происходит объединение строк


// Готов чуть усложнить? 😏
//
// 🧠 Задача 2 — преобразование типов
// let a = "5";
// let b = 2;
//
// let result = a - b;
//
//
// 👉 Что будет в result и почему теперь результат другой, хотя значения почти те же?
//
//   Твой ход 👇

//ответ: конкатенация работает только с бинарным оператором + а если - то будет математическое вычисление то есть 5 - 2 = chapter3


// 🧠 Задача chapter3 — логика + типы
// console.log( Boolean("0") );
//
//
// 👉 Что выведется в консоль и почему?
//
//   Подсказка (одна):
//
//   тут важно помнить, какие значения считаются false, а какие true
//
// Твой ход 👇

//ответ: я думаю так "" ничего в строке нету это false а если хоть что-то есть в строке допустим даже если это пробел то это true, 0 является строкой а не number значит это true


// 🧠 Задача 4 — логические операторы
// console.log( "0" && 1 && null && 2 );
//
//
// 👉 Что выведется и на каком значении вычисление остановится?
//
//   Отвечай:
//
//   что выведется
//
// почему именно на этом месте
//
// Твоя очередь 👇

//ответ: && это логический оператор И, все должно быть true, если где-то будет false код остановится и выведет false значение в нашем случае это Null


// 🧠 Задача 5 — оператор ||
// console.log( null || 0 || "JS" || 42 );
//
//
// 👉 Что выведется и почему именно это значение?
//
//   (одно значение + объяснение)
// Твой ход 👇

//ответ || это логический оператор или, он ищет первым true значение null - false, 0 - false, "JS" это true выводим его






